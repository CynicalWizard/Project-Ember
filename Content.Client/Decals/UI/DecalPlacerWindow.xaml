<DefaultWindow xmlns="https://spacestation14.io"
               Title="{Loc 'decal-placer-window-title'}"
               MinSize="340 620"
               SetSize="340 700">
    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'decal-placer-window-category'}" Margin="0 0 4 0" />
            <OptionButton Name="Category" HorizontalExpand="True" />
        </BoxContainer>
        <LineEdit Name="Search" PlaceHolder="{Loc 'decal-placer-window-search'}" />
        <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
            <GridContainer Name="Grid" Columns="8">
                <!-- Decals get added here by code -->
            </GridContainer>
        </ScrollContainer>
        <Label Name="SelectedLabel" Text="{Loc 'decal-placer-window-nothing-selected'}"
               Margin="0 2 0 2" StyleClasses="LabelSubText" />

        <BoxContainer Orientation="Vertical">
            <ColorSelectorSliders Name="ColorPicker" IsAlphaVisible="True" />
            <Button Name="PickerOpen" Text="{Loc 'decal-placer-window-palette'}" />
        </BoxContainer>
        <CheckBox Name="EnableAuto" Text="{Loc 'decal-placer-window-enable-auto'}" Margin="0 0 0 10"/>
        <CheckBox Name="EnableColor" Text="{Loc 'decal-placer-window-use-color'}" />
        <CheckBox Name="EnableSnap" Text="{Loc 'decal-placer-window-enable-snap'}" />
        <CheckBox Name="EnableCleanable" Text="{Loc 'decal-placer-window-enable-cleanable'}" />
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'decal-placer-window-facing'}" Margin="0 0 4 1" />
            <Button Name="FaceSouth" Text="{Loc 'decal-placer-window-face-south'}" HorizontalExpand="True" />
            <Button Name="FaceEast" Text="{Loc 'decal-placer-window-face-east'}" HorizontalExpand="True" />
            <Button Name="FaceNorth" Text="{Loc 'decal-placer-window-face-north'}" HorizontalExpand="True" />
            <Button Name="FaceWest" Text="{Loc 'decal-placer-window-face-west'}" HorizontalExpand="True" />
        </BoxContainer>
        <BoxContainer Name="SpinBoxContainer" Orientation="Horizontal">
            <Label Text="{Loc 'decal-placer-window-rotation'}" Margin="0 0 0 1" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'decal-placer-window-zindex'}" Margin="0 0 0 1" />
            <SpinBox Name="ZIndexSpinBox" HorizontalExpand ="True" />
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
