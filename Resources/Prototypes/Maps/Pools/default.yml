- type: gameMapPool
  id: DefaultMapPool
  maps:
  # Ember: the upstream station maps were removed. Development runs on the Dev map,
  # which carries exactly one post so branch, rank, skills and job titles get exercised
  # end to end. Ember's own stations go here once they exist.
  - Dev
